[
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\layer.py",
  "line": 1874,
  "constant": "[:500]",
  "stage": "persist",
  "impact": "medium",
  "what": "Auto-deliberation decisions are finalized with only the first 500 chars of the assistant response as brain.decisions.description",
  "why": "The description is the decision's primary searchable/embedded content; the actual conclusion of a long response often sits past char 500, so decision memory stores the preamble instead of the decision.",
  "suggested": "",
  "verify_evidence": "layer.py:1873-1874 `lessons = [reflection[:500]]` -> Heart.end_episode(lessons_learned=lessons) at layer.py:1875-1881 persists to heart.episodes. No related field in nous/config.py (grep for reflection/lesson finds only correction_extraction_enabled:1135 and unrelated 'lesson_learned' subject comment:271). end_session callers pass reflection verbatim; no max-chars threading."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\layer.py",
  "line": 825,
  "constant": "user_input[:200]",
  "stage": "derive",
  "impact": "medium",
  "what": "The episode-dedup embedding probe is computed on only the first 200 chars of the user input",
  "why": "Two inputs that share a 200-char preamble but diverge afterward can false-positive as duplicates, silently skipping episode creation and losing the whole session's episodic capture.",
  "suggested": "",
  "verify_evidence": "layer.py:824-832 `EpisodeInput(summary=user_input[:200], ...)` -> Heart.start_episode persists heart.episodes.summary. Literal in the only production call site; no Settings field for episode summary seed length in nous/config.py. Fallback title use confirmed at context.py:943 (e.summary[:60])."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\layer.py",
  "line": 821,
  "constant": "user_input[:200]",
  "stage": "derive",
  "impact": "medium",
  "what": "The episode-dedup embedding probe is computed on only the first 200 chars of the user input",
  "why": "Two inputs that share a 200-char preamble but diverge afterward can false-positive as duplicates, silently skipping episode creation and losing the whole session's episodic capture.",
  "suggested": "",
  "verify_evidence": "layer.py:821 `_is_duplicate_episode(user_input[:200], ...)`; the probe embedding is computed on this slice at layer.py:1774 and a True return skips episode creation entirely (layer.py:822 vs 823-833). Hardcoded literal, single call site, no Settings wiring."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\layer.py",
  "line": 1783,
  "constant": "> 0.85 (hours=48, limit=1)",
  "stage": "capture",
  "impact": "medium",
  "what": "Cosine threshold + 48h window deciding whether a new episode is a duplicate and therefore never created",
  "why": "A storage-admission threshold not in Settings; too-low a value drops entire episodes (and their transcripts/facts/chunks) at capture time with no telemetry.",
  "suggested": "",
  "verify_evidence": "layer.py:1777-1783: search_recent_episodes_by_embedding(hours=48, limit=1) and `if results and results[0][1] > 0.85: return True` -> episode creation skipped (layer.py:821-822). All three values are call-site literals; nous/config.py has no episode-dedup fields (fact_dedup_threshold:335 and fact_native_cosine_threshold are fact-path only)."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\layer.py",
  "line": 49,
  "constant": "_MIN_CONTENT_LENGTH = 200",
  "stage": "capture",
  "impact": "medium",
  "what": "Significance gate for creating an episode (line 1743) AND the trivial-episode soft-delete gate at end_session (line 1864)",
  "why": "Sessions under 200 combined chars with one turn and no tools are retroactively soft-deleted \u2014 the episode, its transcript, and any downstream fact extraction never persist; not tunable via Settings.",
  "suggested": "",
  "verify_evidence": "layer.py:49 module constant; consumed at layer.py:1743 (_should_create_episode significance gate) and layer.py:1860-1869 (end_session trivial check -> deactivate_episode soft-delete). No Settings read at either site; grep of nous/config.py for min_content finds only cross_type_link_min_content_chars:75 and ce_backfill_min_content_chars:856 (graph-linking, unrelated)."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\layer.py",
  "line": 1180,
  "constant": "[:500]",
  "stage": "persist",
  "impact": "medium",
  "what": "Auto-deliberation decisions are finalized with only the first 500 chars of the assistant response as brain.decisions.description",
  "why": "The description is the decision's primary searchable/embedded content; the actual conclusion of a long response often sits past char 500, so decision memory stores the preamble instead of the decision.",
  "suggested": "",
  "verify_evidence": "layer.py:1178-1180 `self._deliberation.finalize(decision_id, description=turn_result.response_text[:500], ...)` -> deliberation.py:176-184 Brain.update(description=...) persists brain.decisions.description. Literal at the only production call site; no Settings field. Note: the same 500-char description is also what the quality gate (deliberation.py:168) and guardrail check (deliberation.py:192-204"
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\layer.py",
  "line": 801,
  "constant": "user_input[:500]",
  "stage": "persist",
  "impact": "low",
  "what": "Deliberation start records 'Plan: {user_input[:500]}' into brain.decisions",
  "why": "Long user requests lose their tail in the persisted decision description and the dedup probe used by DeliberationEngine._is_duplicate, degrading later decision recall.",
  "suggested": "",
  "verify_evidence": "layer.py:800-803 `_deliberation.start(agent_id, user_input[:500], frame, ...)` -> deliberation.py:113-128 Brain.record(RecordInput(description=f\"Plan: {description}\")) persists brain.decisions; same slice feeds _is_duplicate (deliberation.py:106-108, embed at :267). Hardcoded, single call site, no config."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\layer.py",
  "line": 1157,
  "constant": "thinking[:2000]",
  "stage": "persist",
  "impact": "low",
  "what": "Each captured thinking block persisted as a deliberation trace on the decision is cut at 2000 chars",
  "why": "Extended-thinking blocks routinely exceed 2000 chars; the stored reasoning trace consumed by decision review/quality scoring loses the conclusion of long thoughts.",
  "suggested": "",
  "verify_evidence": "layer.py:1153-1160 `self._deliberation.think(decision_id, thinking[:2000], ...)` -> deliberation.py:143 Brain.think() persists the thought on the decision (brain schema). Literal at the only call site capturing thinking_blocks; nous/config.py thinking_mode/thinking_budget (config.py:415-416) govern API extended-thinking budget, not this persistence truncation."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\layer.py",
  "line": 1628,
  "constant": "content[:200] (score >= 0.7, candidates[:10])",
  "stage": "persist",
  "impact": "medium",
  "what": "Items loaded into heart.working_memory: only score>=0.7, max 10 items, summaries cut to 200 chars",
  "why": "Working memory is re-read as next-turn context; the 0.7 gate / 10-item cap / 200-char summary jointly decide which recalled memories survive across turns, and none are Settings-tunable.",
  "suggested": "",
  "verify_evidence": "layer.py:1612 `if score >= 0.7`, layer.py:1621 `candidates[:10]`, layer.py:1628 `summary=content[:200]` -> Heart.load_to_working_memory at layer.py:1632 persists heart.working_memory rows. nous/config.py working_memory_* fields (config.py:599-612) cover only F049 TTL/sweep, not the 0.7 gate, 10-item cap, or 200-char summary."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\layer.py",
  "line": 1586,
  "constant": "text[:200]",
  "stage": "persist",
  "impact": "low",
  "what": "The working-memory current_task (focus text persisted via Heart.focus) is capped at 200 chars of the user input (also line 1576)",
  "why": "heart.working_memory.current_task drives the Working Memory context section and F038 task synthesis; long task statements are stored truncated mid-sentence.",
  "suggested": "",
  "verify_evidence": "layer.py:1576 and layer.py:1586 `return text[:200]` from _resolve_focus_text -> Heart.focus(session_id, focus_text, ...) at layer.py:846/854 persists heart.working_memory.current_task. Literal, no Settings wiring (working_memory_* config fields are TTL/sweep only, config.py:599-612)."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\layer.py",
  "line": 1948,
  "constant": "reflection[:1000] (and reflection[:200] at 1933)",
  "stage": "derive",
  "impact": "low",
  "what": "The reflection payload carried in the session_ended event summary consumed by the rubric outcome detector (also reflection[:200] at line 1933)",
  "why": "The outcome detector's LLM leg derives stored outcome signals from this field; reflections over 1000 chars lose their tail before signal classification.",
  "suggested": "",
  "verify_evidence": "layer.py:1933 and layer.py:1948 truncate reflection in the session_ended event payload; handlers/outcome_detector.py:79-104 feeds summary (json[:2000] at :133) to the LLM and persists OutcomeSignal rows to heart.outcome_signals (models.py:725-737, schema='heart'). Both truncations are literals; only rubric_outcome_detection_enabled/rubric_outcome_model are in Settings (outcome_detector.py:68,140),"
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\monitor.py",
  "line": 345,
  "constant": "max_tokens=512",
  "stage": "derive",
  "impact": "medium",
  "what": "Output budget for the F039 inline-correction extraction LLM call whose JSON becomes a persisted fact (and optionally a censor)",
  "why": "Same bug class as F031's max_tokens=300: if the model emits verbose JSON the principle/censor_pattern fields truncate and parse_llm_json fails, silently dropping the correction fact the user explicitly gave.",
  "suggested": "",
  "verify_evidence": "monitor.py:340-346 call_background_llm(..., max_tokens=512) whose parsed JSON becomes a persisted fact via Heart.learn (monitor.py:366-375) and optionally a censor (monitor.py:380-386). Truncated JSON -> parse_llm_json failure -> except at monitor.py:390-392 returns None (correction silently dropped). Only correction_extraction_enabled (config.py:1135) is in Settings; no max_tokens field."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\monitor.py",
  "line": 328,
  "constant": "[:1000]",
  "stage": "derive",
  "impact": "medium",
  "what": "The correction-extraction LLM sees only the first 1000 chars of the user message and of the AI response",
  "why": "If the actual correction appears after char 1000 (long pasted message or long AI answer), the derived rule-fact is extracted from context that omits it, storing a wrong or empty principle.",
  "suggested": "",
  "verify_evidence": "monitor.py:328-329 `User: {user_message[:1000]}` / `AI response: {ai_response[:1000]}` \u2014 the only context the F039 extraction LLM sees before the derived principle is persisted to heart.facts at monitor.py:375. Literals, no Settings wiring."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\monitor.py",
  "line": 353,
  "constant": "len(principle) < 30",
  "stage": "capture",
  "impact": "low",
  "what": "Minimum-length admission gate on the extracted correction principle before it is stored as a fact",
  "why": "Short but valid corrections ('Always use uv, not pip.' = 24 chars) are silently discarded and never enter heart.facts.",
  "suggested": "",
  "verify_evidence": "monitor.py:352-354 `if not principle or len(principle) < 30: return None` \u2014 admission gate before Heart.learn at monitor.py:375; a short valid correction never reaches heart.facts. Literal; nothing in nous/config.py governs it (fact 30-char min in F038 is a different gate in the heart/facts path)."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\monitor.py",
  "line": 265,
  "constant": "[:50] and pairs[:5]",
  "stage": "derive",
  "impact": "medium",
  "what": "Error text fed to the F012 recovery-procedure LLM is 50 chars per error, max 5 pairs (errors already pre-cut to [:200] at line 196, recovery context to [:200] at line 211)",
  "why": "The stored recovery procedure (heart.procedures) is derived from 50-char error stubs that usually cut off the actual error cause, degrading a permanently persisted procedural memory.",
  "suggested": "",
  "verify_evidence": "monitor.py:264-269 error[:50] per pair, pairs[:5]; errors pre-cut [:200] at monitor.py:196, recovery context [:200] at monitor.py:211. Output stored via store_procedure -> heart.procedures at monitor.py:295. Settings DO gate trigger count and per-session cap (procedure_monitor_trigger_count / procedure_max_per_session via getattr at monitor.py:216-218) but NOT the truncation literals."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\monitor.py",
  "line": 402,
  "constant": "[:100]",
  "stage": "persist",
  "impact": "low",
  "what": "Auto-created censor trigger patterns embed only the first 100 chars of the tool error (plus first 3 args at line 399)",
  "why": "The censor persisted to heart.censors matches future inputs on this truncated pattern; a cut-off error string weakens both matching precision and the stored guardrail's meaning.",
  "suggested": "",
  "verify_evidence": "monitor.py:399 args [:3] and monitor.py:402 `error_snippet = (tool_result.error or \"\")[:100]` build the censor candidate text; persisted verbatim as trigger_pattern via Heart.add_censor at monitor.py:172-178 -> heart.censors. Literals, no Settings field."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\monitor.py",
  "line": 45,
  "constant": "_MAX_CENSORS_PER_SESSION = 3",
  "stage": "capture",
  "impact": "low",
  "what": "Hard cap on auto-created censors persisted per session",
  "why": "In a session with many distinct non-transient failures, the 4th+ failure pattern is never learned as a censor; not Settings-tunable.",
  "suggested": "",
  "verify_evidence": "monitor.py:45 module constant, enforced at monitor.py:162 `if session_count >= _MAX_CENSORS_PER_SESSION: break` \u2014 caps auto-censor persistence to heart.censors per session. Unlike the procedure caps (Settings via getattr at monitor.py:216-218), this one has no Settings read anywhere."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\deliberation.py",
  "line": 33,
  "constant": "_DEDUP_EMBEDDING_THRESHOLD = 0.85 (_DEDUP_WINDOW_MINUTES=5, _DEDUP_KEYWORD_THRESHOLD=0.5)",
  "stage": "capture",
  "impact": "medium",
  "what": "Whether a new deliberation decision is recorded to brain.decisions at all \u2014 >0.85 similarity to any decision in the last 5 min suppresses the record",
  "why": "A dedup admission threshold on decision memory that lives outside Settings; the keyword fallback at 0.5 containment is aggressive and can silently drop genuinely distinct decisions on the same topic.",
  "suggested": "",
  "verify_evidence": "deliberation.py:32-34 module constants used at :257 (window), :274 (embedding >0.85 -> True), :291 (keyword containment >0.5 -> True); a True from _is_duplicate at :106-110 returns None before Brain.record \u2014 the decision is never written to brain.decisions. DeliberationEngine holds settings (:78-80) but never reads dedup values from it; no matching fields in nous/config.py."
 },
 {
  "area": "cognitive",
  "file": "E:\\Projects\\nous\\nous\\cognitive\\deliberation.py",
  "line": 50,
  "constant": "< 20",
  "stage": "capture",
  "impact": "low",
  "what": "Quality-gate minimum description length; failing decisions are DELETED at finalize (line 173)",
  "why": "A terse but real decision response under 20 chars causes the whole deliberation record (including captured thinking trace) to be hard-deleted from brain.decisions.",
  "suggested": "",
  "verify_evidence": "deliberation.py:50-51 quality-gate rule; on rejection finalize() hard-deletes the whole decision record at deliberation.py:171-174 (self.delete -> Brain.delete, deliberation.py:220-231). Literal; config.py:54-56 comment confirms the only quality Settings field (quality_block_threshold) was REMOVED \u2014 no wiring exists."
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 299,
  "constant": "[:500]",
  "stage": "derive",
  "impact": "medium",
  "what": "F377 dedup tiebreaker (DUPLICATE vs DISTINCT) compares only the first 500 chars of each fact",
  "why": "A candidate fact whose materially different claim (value, sign, date) appears after char 500 looks like a paraphrase, gets verdict DUPLICATE, and is never stored \u2014 silent information loss at the write gate.",
  "suggested": "fact_classifier_input_chars",
  "verify_evidence": "facts.py:298-301 hardcodes old_content[:500]/new_content[:500] in _classify_fact_pair. Verdict drives destructive writes at all three claimed sites: _find_contradiction facts.py:777 -> superseded_by/active=False/confidence*0.3 at 813-817, 826-827, 844-846; _supersede_by_subject facts.py:948 -> supersede at 970-971; _classify_dupe_in_band facts.py:1034 -> _apply_band_action 1096-1109. No input-char"
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 331,
  "constant": "[:500]",
  "stage": "derive",
  "impact": "medium",
  "what": "F377 dedup tiebreaker (DUPLICATE vs DISTINCT) compares only the first 500 chars of each fact",
  "why": "A candidate fact whose materially different claim (value, sign, date) appears after char 500 looks like a paraphrase, gets verdict DUPLICATE, and is never stored \u2014 silent information loss at the write gate.",
  "suggested": "fact_classifier_input_chars",
  "verify_evidence": "facts.py:330-333 hardcodes existing[:500]/candidate[:500] in is_distinct_fact. Production callers: handlers/fact_extractor.py:132 and handlers/knowledge_extractor.py:135 (gated by fact_dedup_tiebreaker_enabled, config.py:351, default False); a DUPLICATE/None verdict means the candidate is deduped away, never stored. The feature toggle is configurable but the 500-char classifier input cap has no Se"
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 311,
  "constant": "max_tokens=300",
  "stage": "derive",
  "impact": "low",
  "what": "Output budget for the F027 supersession classifier structured call",
  "why": "Output is a 3-field enum/number schema so 300 is likely sufficient, but this is the exact bug class that silently downgraded 91% of F031 merges (max_tokens=300); a model that emits reasoning text before the tool call would exhaust it and fail the classification open to a blind ContradictionWarning.",
  "suggested": "",
  "verify_evidence": "facts.py:311 hardcodes max_tokens=300 in the call_background_llm_structured call for the F027 supersession classifier. No max-tokens field for this call in nous/config.py; callers (facts.py:777, 948, 1034) pass no budget. Failure/None falls open to the plain ContradictionWarning (facts.py:849-855) or confirm-dedup (1035-1036), as claimed."
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 344,
  "constant": "max_tokens=100",
  "stage": "derive",
  "impact": "low",
  "what": "Output budget for the F377 dedup tiebreaker structured call",
  "why": "Single-enum output normally fits, but exhaustion returns None which fails open to DEDUP \u2014 i.e. the candidate fact is silently merged away rather than stored; same F031-class silent-downgrade shape.",
  "suggested": "",
  "verify_evidence": "facts.py:344 hardcodes max_tokens=100 for the F377 tiebreaker; None/malformed result returns None (facts.py:350-352) and callers fail open to dedup (fact_extractor.py:130-138 stores only on literal True; knowledge_extractor.py:134-137 marks duplicate) \u2014 candidate is silently not stored, exactly the claimed shape. No Settings wiring."
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 506,
  "constant": "< 30",
  "stage": "persist",
  "impact": "high",
  "what": "F038-1.2 hard floor: any fact under 30 chars is rejected before dedup/admission (mirrored at line 437 for utility precompute)",
  "why": "Terse but high-value facts ('v0.2 shipped', 'Tim is in Austin', names, IDs, dates) are silently rejected on every write path including user_stated bypass sources, permanently excluding them from all downstream recall.",
  "suggested": "fact_min_content_chars",
  "verify_evidence": "facts.py:506 hard-rejects content <30 chars at the top of _learn before dedup/admission (bypass sources included \u2014 admission.py:417-419 comment confirms 'the <30-char content floor already runs upstream in Heart._learn'); mirrored at facts.py:437 for the W-1 utility precompute. No fact_min_content_chars or equivalent exists in nous/config.py (grep for min_content matches only cross_type_link_min_c"
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 144,
  "constant": "CONTRADICTION_SIMILARITY_MIN = 0.85 / CONTRADICTION_SIMILARITY_MAX = 0.95",
  "stage": "persist",
  "impact": "medium",
  "what": "Cosine band inside which write-time contradiction detection runs (used at line 745 params and line 1062 band routing)",
  "why": "Contradicting facts below 0.85 similarity are never checked at write time, and the band interacts with the env-tunable fact_native_cosine_threshold \u2014 an operator who moves the dedup threshold cannot move this band, so contradictions in the excluded range accumulate unresolved.",
  "suggested": "fact_contradiction_similarity_min/max",
  "verify_evidence": "Class constants at facts.py:144-145, consumed at facts.py:745-746 (write-time band SQL), 1027 and 1062 (band routing in _classify_dupe_in_band). Notably config.py:724 DOES define contradiction_similarity_threshold=0.85 but grep across nous/**/*.py shows it is consumed NOWHERE \u2014 a dead Settings field, so the operator knob the scanner asks for nominally exists but is unwired. The interaction with en"
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 767,
  "constant": "LIMIT 1",
  "stage": "persist",
  "impact": "medium",
  "what": "_find_contradiction examines only the single nearest in-band neighbor per learned fact",
  "why": "If the nearest in-band fact is UNRELATED but the 2nd-nearest genuinely contradicts the new fact, the contradiction is never detected or linked \u2014 contradictory pairs persist untagged until the sleep-cycle scan (which is itself capped) maybe catches them.",
  "suggested": "fact_contradiction_candidates_per_learn",
  "verify_evidence": "facts.py:756-768: _find_contradiction SQL ends 'ORDER BY embedding <=> ... LIMIT 1' \u2014 only the single nearest in-band neighbor is examined; row.first() at 771. No candidate-count parameter, no Settings field, sole caller facts.py:701 passes none."
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 945,
  "constant": "similarity > 0.80",
  "stage": "persist",
  "impact": "high",
  "what": "Same-subject supersession threshold in _supersede_same_subject (LLM disambiguation band bounded by `similarity <= 0.95` at line 947)",
  "why": "This gate deactivates old facts (active=False, superseded_by set); it is not in Settings even though the sibling dedup threshold (fact_native_cosine_threshold) is env-tunable, so operators tuning dedup cannot tune the more destructive supersession trigger, and if the LLM client is absent every same-subject pair above 0.80 is superseded with no disambiguation.",
  "suggested": "fact_subject_supersession_threshold",
  "verify_evidence": "facts.py:945 hardcodes 0.80 in _supersede_by_subject (scanner's name '_supersede_same_subject' is slightly off; code matches the claim). LLM disambiguation gated at facts.py:947 requires similarity<=0.95 AND self._llm is not None \u2014 with no LLM client every same-subject pair >0.80 falls straight to active=False/superseded_by at 970-971, as claimed. Sibling dedup threshold IS tunable (fact_native_co"
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 817,
  "constant": "* 0.3",
  "stage": "retrieve",
  "impact": "low",
  "what": "Retrieval soft-suppression penalty on superseded facts whose superseder is absent from the result set (apply_supersession_filter; low-confidence facts also get score *= confidence when confidence < 0.5)",
  "why": "A hardcoded 70% score cut effectively buries superseded facts below any relevance floor, so historical versions rarely surface even when the query is explicitly about the past; no operator knob exists to soften it for temporal-reasoning use cases.",
  "suggested": "fact_superseded_score_penalty",
  "verify_evidence": "facts.py:817 (UPDATE/current=new path) and facts.py:1099 (_apply_band_action supersede_old) both hardcode confidence = max(0.0, (confidence or 1.0) * 0.3) on the stored heart.facts row. Consumed downstream by apply_supersession_filter's confidence<0.5 scaling (facts.py:382-384). No Settings field."
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 846,
  "constant": "- 0.2",
  "stage": "persist",
  "impact": "low",
  "what": "Confidence decrement applied to the existing fact on a CONTRADICTION verdict (repeated at line 1109)",
  "why": "Each contradiction permanently subtracts 0.2 from the stored fact's confidence, which then down-weights it in every future recall via the confidence penalty in apply_supersession_filter.",
  "suggested": "",
  "verify_evidence": "facts.py:846 (CONTRADICTION verdict in _find_contradiction) and facts.py:1109 (_apply_band_action contradiction) both hardcode the -0.2 permanent decrement on the stored fact's confidence. Down-weights future recall via facts.py:382-384. No Settings field."
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 1210,
  "constant": "LIMIT 20",
  "stage": "persist",
  "impact": "low",
  "what": "_find_duplicate dedup candidate horizon \u2014 the F075 same-date preference and threshold filter only see the top-20 nearest facts (ef_search hardcoded 100 at line 1200)",
  "why": "A same-date duplicate ranked 21+ by cosine is invisible to the date preference, so a duplicate can be inserted; the code comments acknowledge the horizon but nothing lets an operator widen it as the corpus densifies.",
  "suggested": "fact_dedup_candidate_horizon",
  "verify_evidence": "facts.py:1210 hardcodes LIMIT 20 in _find_duplicate; ef_search hardcoded 100 at facts.py:1200; the threshold applied over the top-20 IS configurable (fact_native_cosine_threshold, facts.py:1179-1182) but the horizon is not \u2014 code comment at 1169-1173 explicitly acknowledges 'a same-date duplicate ranked 21+ by cosine is invisible'. No fact_dedup_candidate_horizon or equivalent in config.py."
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 2157,
  "constant": "> 0.75 ... < 0.95",
  "stage": "derive",
  "impact": "medium",
  "what": "Similarity band for sleep-cycle contradiction candidates (find_contradiction_candidates, default limit=10 at line 2090)",
  "why": "This band plus the LIMIT gates how many fact pairs per sleep cycle the F031 resolver may merge/resolve; contradicting same-subject pairs below 0.75 similarity are structurally unreachable by the entire contradiction pipeline (write-time band starts at 0.85, this one at 0.75, nothing below).",
  "suggested": "contradiction_scan_similarity_min/max",
  "verify_evidence": "facts.py:2157-2158 hardcode the 0.75-0.95 band in _find_contradiction_candidates; production caller sleep_handler.py:1011 passes hardcoded limit=10 (the default at facts.py:2090). The cooldown IS configurable (contradiction_recheck_cooldown_days, facts.py:2121-2124, config.py:726) but band and limit are not. The structural gap below 0.75 (write-time band starts at 0.85, sleep scan at 0.75) is accu"
 },
 {
  "area": "heart",
  "file": "nous/heart/facts.py",
  "line": 379,
  "constant": "* 0.3",
  "stage": "retrieve",
  "impact": "low",
  "what": "Retrieval soft-suppression penalty on superseded facts whose superseder is absent from the result set (apply_supersession_filter; low-confidence facts also get score *= confidence when confidence < 0.5)",
  "why": "A hardcoded 70% score cut effectively buries superseded facts below any relevance floor, so historical versions rarely surface even when the query is explicitly about the past; no operator knob exists to soften it for temporal-reasoning use cases.",
  "suggested": "fact_superseded_score_penalty",
  "verify_evidence": "facts.py:379 hardcodes the 0.3 soft-suppression multiplier in apply_supersession_filter (359-390), invoked on live search paths at facts.py:1741 and 1838 (search/find_similar summaries). Retrieve-stage as scanner labeled \u2014 never mutates stored rows. No fact_superseded_score_penalty in config.py; retrieval_pipeline.py:1201 even hardcodes a mirror ('stale_penalty: float = 0.3 # mirrors apply_superse"
 },
 {
  "area": "heart",
  "file": "nous/heart/heart.py",
  "line": 936,
  "constant": "limit * 2",
  "stage": "retrieve",
  "impact": "medium",
  "what": "Per-type candidate pool fetched from each memory manager before cross-type merge/CE/MMR in Heart._recall",
  "why": "With 4 types competing, a type whose relevant hits sit just past 2x the caller limit never enters the merge, and the CE reranker / MMR only ever see this pre-truncated pool \u2014 the multiplier silently bounds recall quality for every recall_deep call.",
  "suggested": "recall_fetch_multiplier",
  "verify_evidence": "heart.py:936 'fetch_limit = limit * 2  # Fetch more for merging' feeds every per-type search_map entry (heart.py:941-948) in Heart._recall, the single recall funnel behind Heart.recall (heart.py:840/875/884). Caller limit is a parameter but the x2 multiplier has no Settings field (no recall_fetch_multiplier or similar in config.py) and no call site threads one."
 },
 {
  "area": "heart",
  "file": "nous/heart/search.py",
  "line": 236,
  "constant": "limit * 3",
  "stage": "retrieve",
  "impact": "medium",
  "what": "Vector and keyword channel candidate pools feeding RRF fusion in hybrid_search (penalty_rank = limit + 1 at line 152 is the paired constant)",
  "why": "A document ranked 3*limit+1 in both channels is invisible to RRF regardless of true relevance; every Heart memory type's hybrid retrieval inherits this pool size with no Settings override, and it compounds with heart.py's limit*2 upstream.",
  "suggested": "hybrid_search_pool_multiplier",
  "verify_evidence": "search.py:236 hardcodes \"limit_expanded\": limit * 3 used by both the vector SQL (search.py:255) and keyword leg; paired penalty_rank = limit + 1 at search.py:152. hybrid_search DOES resolve vector_weight and rrf_k from settings/runtime (search.py:228-230) but the pool multiplier has no resolver, no Settings field, no caller override."
 },
 {
  "area": "heart",
  "file": "nous/heart/episodes.py",
  "line": 32,
  "constant": "_DEDUP_WINDOW_MINUTES = 30 / _DEDUP_THRESHOLD = 0.80",
  "stage": "capture",
  "impact": "medium",
  "what": "Episode-start dedup: a new episode whose summary text_overlap with an ongoing episode exceeds 0.80 within 30 minutes is silently merged into the existing episode (used at lines 73/82)",
  "why": "Two genuinely distinct sessions with similar opening summaries collapse into one episode row at capture time \u2014 the second session's transcript, summary, facts and chunks all attach to the wrong parent episode, poisoning every downstream derivation.",
  "suggested": "episode_dedup_window_minutes / episode_dedup_threshold",
  "verify_evidence": "episodes.py:32-33 class constants, consumed at episodes.py:73 (window cutoff) and 82 (text_overlap > threshold -> silently reuse existing episode, returning the OLD row at 84-85 so all downstream writes attach to it). EpisodeManager.__init__ (episodes.py:35-43) takes no settings; heart.py:107 constructs it with only (db, embeddings, agent_id). No episode_dedup_* fields in config.py. Note: comparis"
 },
 {
  "area": "heart",
  "file": "nous/heart/working_memory.py",
  "line": 134,
  "constant": "or 20",
  "stage": "persist",
  "impact": "medium",
  "what": "Working-memory item capacity fallback (repeated as row_cap at line 521 and in _to_state at line 607); at capacity _evict_lowest silently drops the lowest-relevance item",
  "why": "The 20-item cap bounds what session context survives into the next turn's prompt (curated loads compete with F055 residual entries for the same 20 slots); evicted items are gone from heart.working_memory with no configurable ceiling.",
  "suggested": "working_memory_max_items",
  "verify_evidence": "working_memory.py:134 (load_item eviction cap, _evict_lowest at 137-138), 521 (residual row_cap), 607 (_to_state) all fall back to 20. The DB column max_items exists (storage/models.py:822, server_default=\"20\") but grep shows NO code path ever writes it and no Settings field feeds it \u2014 WorkingMemoryManager is constructed with only (database, agent_id) at heart.py:122. Config has working_memory_ttl"
 },
 {
  "area": "heart",
  "file": "nous/heart/residual_activation.py",
  "line": 260,
  "constant": "snippet[:160]",
  "stage": "persist",
  "impact": "medium",
  "what": "Content persisted into heart.working_memory JSONB for each residually-surfaced memory item",
  "why": "The 160-char head is the ONLY content stored for a residual item and is what renders into subsequent turns' prompts \u2014 a fact/episode whose key detail sits past char 160 carries forward as a useless stub, defeating the cross-turn train-of-thought mechanism it implements.",
  "suggested": "residual_item_summary_chars",
  "verify_evidence": "residual_activation.py:260 hardcodes snippet[:160] as the ONLY content in the entry dict persisted via upsert_residual_items (residual_activation.py:266-270) into heart.working_memory.items JSONB (working_memory.py:528/546). Sibling residual_* knobs ARE in Settings (residual_top_k_carried read at residual_activation.py:237; config.py:1158-1191) but no summary-length field exists."
 },
 {
  "area": "heart",
  "file": "nous/heart/procedures.py",
  "line": 60,
  "constant": "_EMBED_CHAR_CAP = 16000",
  "stage": "derive",
  "impact": "low",
  "what": "Procedure embedding input \u2014 name/description/patterns/goals/tools/concepts/notes concatenation truncated at 16000 chars before embed",
  "why": "For large F081 skill bodies, content past 16K chars never influences the stored embedding, so vector search cannot match on late-body concepts; deliberate (8191-token embed API cap) but CJK-dense bodies can still overflow and the 2-chars/token assumption is unverified per-corpus.",
  "suggested": "",
  "verify_evidence": "procedures.py:60-61: function-local constant in _build_embed_text truncating the embedding input (stored body stays full per comment at 52-59). ProcedureManager.__init__ (procedures.py:67-84) receives utility_boost/alpha/beta/min_activations from Settings (heart.py:112-120) but no embed cap; no related field in config.py. Affects the stored heart.procedures embedding as claimed."
 },
 {
  "area": "heart",
  "file": "nous/heart/admission.py",
  "line": 24,
  "constant": "DEFAULT_TYPE_PRIORS = {\"rule\": 0.95, ... \"concept\": 0.60}",
  "stage": "persist",
  "impact": "medium",
  "what": "Category priors for the most influential admission dimension (weight 0.30); unknown/missing categories fall to 0.50 at line 264 (`return self.config.type_priors.get(fact_input.category, 0.50)`)",
  "why": "Heart.__init__ wires weights and threshold from Settings but NOT type_priors, so any new fact category the extractor invents silently gets the 0.50 unknown prior and is systematically down-scored at the admission gate \u2014 the exact failure mode that twice required code PRs (F075 'event', PR #524 'status') instead of a config change.",
  "suggested": "admission_type_priors (JSON dict)",
  "verify_evidence": "admission.py:24-44 module dict; AdmissionConfig.type_priors defaults to it (admission.py:110); unknown categories fall to 0.50 at admission.py:264. The claim's key assertion verified: heart.py:89-102 wires weights, threshold, recency_lambda, utility_llm_enabled/model, shadow_mode from Settings but does NOT pass type_priors \u2014 and config.py has admission_w_* / admission_threshold (893-903) but no ad"
 },
 {
  "area": "heart",
  "file": "nous/heart/admission.py",
  "line": 71,
  "constant": "UNGROUNDED_CONFIDENCE_FLOOR = 0.3",
  "stage": "persist",
  "impact": "low",
  "what": "Confidence cap for facts arriving with no source_text from a source not in SOURCE_PENALTIES (applied at line 309)",
  "why": "Any NEW producer path not registered in the hardcoded SOURCE_PENALTIES dict (lines 77-89) has its facts' confidence capped at 0.3 in the composite, pushing them toward rejection \u2014 the PR #380 review already caught two such silently-penalized production callers.",
  "suggested": "admission_ungrounded_confidence_floor",
  "verify_evidence": "admission.py:71 module constant, applied at admission.py:309 (min(UNGROUNDED_CONFIDENCE_FLOOR, fact_input.confidence) when source_text absent AND source not in SOURCE_PENALTIES dict at 77-89). Not a field on AdmissionConfig (105-139), so heart.py:89-102 cannot wire it; no config.py field."
 },
 {
  "area": "heart",
  "file": "nous/heart/admission.py",
  "line": 347,
  "constant": "max_tokens=10",
  "stage": "derive",
  "impact": "low",
  "what": "Output budget for the LLM utility-scoring call in admission (dimension weight 0.25)",
  "why": "If the model prefixes any text before the number, the truncated/unparseable response silently degrades to the 0.5 neutral score (line 355), flattening the highest-weighted-after-type-prior admission dimension for every fact.",
  "suggested": "",
  "verify_evidence": "admission.py:344-348 hardcodes max_tokens=10 in _llm_utility_score; unparseable response degrades to 0.5 at admission.py:350-355 exactly as claimed. Model IS configurable (config.utility_llm_model, wired heart.py:100) but the token budget is not."
 },
 {
  "area": "heart",
  "file": "nous/heart/admission.py",
  "line": 456,
  "constant": "+ 0.15",
  "stage": "persist",
  "impact": "low",
  "what": "F038-2.4 flat admission bonus for agent-initiated user_direct facts",
  "why": "The bonus size decides whether borderline user-directed facts clear the (settings-tunable) threshold, but the bonus itself is not tunable, so raising the threshold silently changes the effective privilege of user_direct facts.",
  "suggested": "admission_user_direct_bonus",
  "verify_evidence": "admission.py:455-456: composite = min(1.0, composite + 0.15) for source=='user_direct', applied after the Settings-wired weighted composite (451-452) and before comparison against the Settings-tunable threshold (admission_threshold, config.py:895, wired heart.py:97). The bonus itself has no AdmissionConfig field and no config.py field."
 },
 {
  "area": "heart",
  "file": "nous/heart/actionability.py",
  "line": 207,
  "constant": "content[:500]",
  "stage": "derive",
  "impact": "medium",
  "what": "F047 LLM actionability classifier input \u2014 only the first 500 chars of a fact are classified",
  "why": "The resulting actionable verdict is PERSISTED on heart.facts.actionable and consumed by heartbeat triage; a long fact whose pending-action clause sits past char 500 is permanently stored as non-actionable and will never page the user.",
  "suggested": "actionability_input_chars",
  "verify_evidence": "actionability.py:207 hardcodes content[:500] in the tier-2 LLM prompt. Verdict IS persisted: facts.py:617-619 calls classify at learn time and writes actionable=actionable_verdict onto the Fact row at facts.py:648; backfill writes SET actionable at handlers/actionability_backfill.py:209; consumed by heartbeat triage (heartbeat/checks.py:283-287, 473-476). Config has actionability_enabled/model/def"
 },
 {
  "area": "heart",
  "file": "nous/heart/censors.py",
  "line": 38,
  "constant": "_MAX_REGEX_INPUT_LEN = 10_000",
  "stage": "retrieve",
  "impact": "low",
  "what": "Text evaluated against censor trigger/unblock regexes is truncated to 10K chars (applied at line 50)",
  "why": "A censor (guardrail memory) whose trigger pattern would match past char 10,000 of a long prompt/tool-output silently never fires \u2014 the stored guardrail exists but is unreachable for long inputs; deliberate ReDoS guard but size not operator-tunable.",
  "suggested": "",
  "verify_evidence": "censors.py:38 module constant, applied at censors.py:50 in _safe_regex_match \u2014 trigger/unblock patterns never see text past 10K chars, so a stored censor cannot fire on late content. Retrieve/enforcement-stage as scanner labeled (no stored-memory mutation). No Settings field; CensorManager.__init__ (censors.py:57-65) takes no settings."
 },
 {
  "area": "heart",
  "file": "nous/heart/censors.py",
  "line": 266,
  "constant": "\"threshold\": 0.7",
  "stage": "retrieve",
  "impact": "medium",
  "what": "Cosine similarity floor for semantic censor matching in the enforcement path _semantic_match (duplicated in read-only _semantic_search at line 396)",
  "why": "Whether a stored censor fires on semantically-related (but not keyword-matching) input hinges entirely on this untunable 0.7 \u2014 too high and learned guardrails silently never trigger; there is no Settings analog despite every comparable graph/dedup threshold being env-tunable.",
  "suggested": "censor_semantic_match_threshold",
  "verify_evidence": "censors.py:266 hardcodes 0.7 in the enforcement path _semantic_match (SQL floor at censors.py:286), duplicated at censors.py:396 in read-only _semantic_search. No censor_semantic_* field in config.py and CensorManager receives no settings object (heart.py:121 constructs with db/embeddings/agent_id only). Claim correct that this is the sole gate for semantic censor firing."
 },
 {
  "area": "heart",
  "file": "nous/heart/query_expansion.py",
  "line": 47,
  "constant": "_MAX_QUERY_LEN = 500",
  "stage": "retrieve",
  "impact": "low",
  "what": "F050 expansion gate: queries longer than 500 chars skip expansion entirely (line 274) and sanitized queries/variants truncate at 500/200 chars (lines 303, 410)",
  "why": "Long, detail-rich recall queries \u2014 precisely the ones that benefit most from multi-query expansion \u2014 silently lose the expansion leg, and generated variants are clipped to 200 chars before embedding.",
  "suggested": "query_expansion_max_query_chars",
  "verify_evidence": "query_expansion.py:47-48 module constants; gate at query_expansion.py:274 (len > _MAX_QUERY_LEN -> skip expansion), sanitized-query truncation at 303. F050 Settings cover model/timeout/variants/min_words/budget/ttl (config.py:1196-1221; min_words read at query_expansion.py:283) but there is no max-query-chars or max-variant-chars field. Retrieve-stage only, as the scanner labeled."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 709,
  "constant": "[:200]",
  "stage": "derive",
  "impact": "high",
  "what": "Each episode summary is cut to 200 chars before being fed to the sleep reflection LLM",
  "why": "Reflection facts, patterns, lessons and UPDATES-supersessions are all derived from 200-char snippets of summaries that are themselves up to 150 words \u2014 the cross-session pattern recognizer never sees most of what happened, degrading every heart.facts row with source='sleep_reflection'.",
  "suggested": "reflection_episode_summary_chars",
  "verify_evidence": "sleep_handler.py:708-709 builds episodes_text from ep.summary[:200]; feeds _REFLECTION_PROMPT whose facts are persisted via heart.learn at :754 (source='sleep_reflection'). No reflection_* field in nous/config.py (grep for 'reflection' returns nothing); self._settings only used for background_model at :724."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 1026,
  "constant": "[:500]",
  "stage": "derive",
  "impact": "high",
  "what": "Contradiction-resolution LLM sees only the first 500 chars of each fact",
  "why": "SUPERSEDE/REMOVE/MERGE verdicts (which deactivate or replace facts permanently) and the MERGE merged_content that becomes the sole surviving fact are computed from truncated fact bodies \u2014 long facts can be wrongly superseded or merged into content missing their tail.",
  "suggested": "contradiction_content_chars",
  "verify_evidence": "sleep_handler.py:1026-1027 content_a/content_b=pair['contentN'][:500] into _CONTRADICTION_RESOLUTION_PROMPT; verdicts drive _apply_supersede (:974-1004) and MERGE writes. config.py has contradiction_similarity_threshold (:724) and contradiction_recheck_cooldown_days (:726) but no content-truncation field."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/episode_summarizer.py",
  "line": 585,
  "constant": "return 3000 / return 1500",
  "stage": "derive",
  "impact": "high",
  "what": "max_tokens for the single episode-summarization LLM call (summary + key_points + candidate_facts JSON)",
  "why": "This is the F031 bug class at the single most upstream derivation seam: if the JSON truncates, candidate_facts at the tail are silently lost (the code even has a known non-dict-parse skip path for exactly this drift), so the episode summary, extracted facts, and everything downstream degrade; the budget is hardcoded, not in Settings despite two flags toggling between two literals.",
  "suggested": "episode_summary_max_tokens",
  "verify_evidence": "episode_summarizer.py:577-586 _summary_max_tokens returns literal 3000 or 1500 chosen by extraction_coverage_broadened/episode_open_threads flags; the VALUES are not Settings fields (config.py:1451/1464 define the flags/stable-limit only, no token-budget field). Used at :602/:609 for the call whose JSON becomes structured_summary + candidate_facts."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/knowledge_extractor.py",
  "line": 203,
  "constant": "[:12000]",
  "stage": "derive",
  "impact": "high",
  "what": "Pre-compaction fact extraction sees only the first 12000 chars of the messages about to be compacted",
  "why": "These messages are about to be destroyed by compaction \u2014 any fact stated after char 12000 of the snapshot is unrecoverable; head-truncation also drops the most recent (usually most fact-dense) turns since the snapshot is chronological.",
  "suggested": "knowledge_extraction_max_chars",
  "verify_evidence": "knowledge_extractor.py:202-203 head-truncates conversation_text at 12000 before the extraction LLM; stored via heart.learn at :152. No knowledge_extraction_* field in config.py; self._settings used only for background_model (:209) and fact_dedup_tiebreaker_enabled (:128)."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/knowledge_extractor.py",
  "line": 181,
  "constant": "[:2000]",
  "stage": "capture",
  "impact": "medium",
  "what": "Each message in the compaction snapshot is capped at 2000 chars (tool-result parts at 500 via line 189, joined parts re-capped at 2000 via line 191) before the extraction LLM sees it",
  "why": "Same archetype as layer.py's [:500] \u2014 per-message truncation at capture-for-derive time means facts embedded deep in a long user message or tool result never reach the extractor and are lost with compaction.",
  "suggested": "knowledge_extraction_message_chars",
  "verify_evidence": "knowledge_extractor.py:181 content[:2000], :189 tool-part [:500], :191 joined [:2000] \u2014 all literals in static method _serialize_messages (no settings in scope). Output is the extraction input for facts persisted at :152."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/knowledge_extractor.py",
  "line": 132,
  "constant": "0.85",
  "stage": "persist",
  "impact": "medium",
  "what": "Cosine dedup threshold gating whether a pre-compaction fact is stored or skipped as duplicate",
  "why": "Hardcoded 0.85 silently diverges from the tuned settings.fact_dedup_threshold (0.92) used by FactExtractor for the identical decision \u2014 this path over-dedups in the 0.85\u20130.92 band, dropping genuinely new facts that the sibling extractor would store.",
  "suggested": "fact_dedup_threshold (reuse existing)",
  "verify_evidence": "knowledge_extractor.py:132 'if cand.score is None or cand.score <= 0.85: break' \u2014 literal dedup gate deciding store-vs-skip before heart.learn (:152). Divergence claim confirmed: settings.fact_dedup_threshold=0.92 exists (config.py:335) and FactExtractor uses it, but it is never referenced in knowledge_extractor.py."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/knowledge_extractor.py",
  "line": 104,
  "constant": "[:5]",
  "stage": "persist",
  "impact": "medium",
  "what": "Max 5 facts stored per compaction event (also 'Max 5 facts.' in _EXTRACT_PROMPT line 62)",
  "why": "A long compacted stretch of conversation can contain far more than 5 salient facts; excess candidates are discarded and the underlying text is destroyed by compaction \u2014 unlike the summarizer path, this cap was never raised by the coverage fix (cap_candidate_facts is not used here).",
  "suggested": "knowledge_extraction_max_facts",
  "verify_evidence": "knowledge_extractor.py:104 'for fact in candidates[:5]' + prompt 'Max 5 facts.' at :62. cap_candidate_facts is NOT imported here (imports at :18 are LLMClient, call_background_llm, parse_llm_json only), so the settings-aware coverage-fix cap does not apply to this path."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/knowledge_extractor.py",
  "line": 106,
  "constant": "confidence < 0.6",
  "stage": "persist",
  "impact": "medium",
  "what": "Hard admission gate on LLM-assigned confidence for pre-compaction facts",
  "why": "An un-tunable threshold on an uncalibrated LLM self-score decides storage; identical literal duplicated at fact_extractor.py:273, so tuning either requires a code change and they can drift.",
  "suggested": "fact_min_extraction_confidence",
  "verify_evidence": "knowledge_extractor.py:105-106 literal 0.6 gate skipping storage; no fact_min/extraction-confidence field in config.py. Duplicate literal confirmed at fact_extractor.py:273."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/fact_extractor.py",
  "line": 273,
  "constant": "confidence < 0.6",
  "stage": "persist",
  "impact": "medium",
  "what": "Hard confidence gate on LLM-fallback extracted facts before Heart.learn",
  "why": "Facts the extractor scored 0.5 are silently dropped before any admission scoring runs; the threshold is not in Settings and duplicates knowledge_extractor.py:106.",
  "suggested": "fact_min_extraction_confidence",
  "verify_evidence": "fact_extractor.py:272-273 literal 0.6 gate in _store_extracted_facts before heart.learn (:332); no matching field in config.py (grep fact_min/extraction confidence = no hits). Drift-pair with knowledge_extractor.py:106 confirmed."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/fact_extractor.py",
  "line": 442,
  "constant": "confidence=0.8",
  "stage": "persist",
  "impact": "medium",
  "what": "Every summarizer-produced candidate fact is persisted with flat confidence 0.8",
  "why": "Stored confidence feeds admission scoring (admission_w_confidence), retrieval relevance and contradiction handling downstream; a flat literal erases any per-fact confidence signal for the highest-volume fact write path.",
  "suggested": "candidate_fact_default_confidence",
  "verify_evidence": "fact_extractor.py:442 'confidence=0.8,  # Default confidence for LLM-extracted candidates' in _store_candidate_facts FactInput persisted via heart.learn (:450-452) \u2014 the summarizer candidate path (highest-volume writer). No candidate_fact confidence field in config.py."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/fact_extractor.py",
  "line": 485,
  "constant": "max_tokens=1500",
  "stage": "derive",
  "impact": "low",
  "what": "Token budget for the LLM-fallback fact extraction call (JSON array of facts)",
  "why": "F031 bug class: a truncated JSON array fails parse_llm_json and returns [] \u2014 all facts from that episode silently lost; low because the prompt caps output at 5 facts so 1500 usually suffices.",
  "suggested": "fact_extraction_max_tokens",
  "verify_evidence": "fact_extractor.py:485 max_tokens=1500 literal in _extract_facts (LLM-fallback); parse failure at :491-494 returns [] silently. No fact_extraction_max_tokens field in config.py."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/fact_extractor.py",
  "line": 76,
  "constant": "Max 5 facts.",
  "stage": "derive",
  "impact": "medium",
  "what": "Prompt-level cap on facts the LLM-fallback extractor may emit per episode",
  "why": "The 2026-06-14 coverage audit found 30% of salient items missed and raised the summarizer-path caps via Settings, but this fallback prompt still instructs max 5 \u2014 episodes whose summaries lack candidate_facts get the old ceiling.",
  "suggested": "candidate_facts_stable_limit (thread into prompt)",
  "verify_evidence": "fact_extractor.py:76 literal 'Max 5 facts.' closes _EXTRACT_PROMPT (fallback path when summary carries no candidate_facts). candidate_facts_stable_limit (config.py:1464) is threaded into cap_candidate_facts on the STORAGE side (:270) but never into this prompt string, so the LLM emits at most 5 regardless of the raised storage cap \u2014 claim's asymmetry confirmed."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 703,
  "constant": "limit=10",
  "stage": "derive",
  "impact": "medium",
  "what": "Max contradiction pairs examined per sleep cycle",
  "why": "Contradictory fact pairs beyond 10 per night stay live and keep surfacing in recall; backlog drains at most 10/cycle regardless of corpus size.",
  "suggested": "contradiction_candidates_per_cycle",
  "verify_evidence": "sleep_handler.py:703 list_episodes(limit=10) literal bounding the reflection input set; no reflection_episode_limit or similar in config.py."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 739,
  "constant": "[:5]",
  "stage": "persist",
  "impact": "medium",
  "what": "Max 5 reflection facts stored per sleep cycle (prompt also says 'Max 5 facts.' at line 65; fallback lessons capped [:3] at line 799)",
  "why": "Reflection is the only cross-session consolidation writer; excess LLM-produced facts beyond 5 are silently discarded each night with no carryover.",
  "suggested": "reflection_max_facts",
  "verify_evidence": "sleep_handler.py:739 'for fact in structured_facts[:5]' (persist cap on heart.learn writes), prompt 'Max 5 facts.' at :65, fallback lessons [:3] at :799 \u2014 all literals, no Settings field."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 730,
  "constant": "max_tokens=1500",
  "stage": "derive",
  "impact": "medium",
  "what": "Token budget for the structured reflection tool call (patterns + lessons + connections + gaps + summary + up to 5 facts)",
  "why": "F031 bug class on a forced tool_use call with a 6-field schema where 'facts' is schema-last \u2014 budget exhaustion by verbose patterns/lessons truncates exactly the field that gets persisted, silently yielding 0 stored facts.",
  "suggested": "reflection_max_tokens",
  "verify_evidence": "sleep_handler.py:730 max_tokens=1500 literal on call_background_llm_structured for _REFLECTION_SCHEMA (facts schema-last, F031 truncation class); not a Settings field."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 1011,
  "constant": "limit=10",
  "stage": "derive",
  "impact": "medium",
  "what": "Max contradiction pairs examined per sleep cycle",
  "why": "Contradictory fact pairs beyond 10 per night stay live and keep surfacing in recall; backlog drains at most 10/cycle regardless of corpus size.",
  "suggested": "contradiction_candidates_per_cycle",
  "verify_evidence": "sleep_handler.py:1011 find_contradiction_candidates(limit=10) literal per-cycle cap. config.py has contradiction_recheck_cooldown_days (:726) and contradiction_similarity_threshold (:724) but no per-cycle candidate limit."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 1049,
  "constant": "max_tokens=1000",
  "stage": "derive",
  "impact": "medium",
  "what": "Token budget for the F027 multi-fact cluster-merge tool call",
  "why": "Same F031 truncation class on a call whose merged_content replaces up to max_facts source facts; a truncated response is classified llm_malformed and the merge silently doesn't happen \u2014 and unlike the pairwise path this budget was never empirically measured (comment says 'not separately measured').",
  "suggested": "cluster_merge_max_tokens",
  "verify_evidence": "sleep_handler.py:1049 max_tokens=1000 literal (comment block :1038-1048 documents the 300\u21921000 F031 fix inline \u2014 the fix stayed a literal). No contradiction_resolution_max_tokens field in config.py."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 1070,
  "constant": "confidence < 0.7",
  "stage": "derive",
  "impact": "medium",
  "what": "Safety floor: any contradiction resolution below 0.7 confidence is downgraded to KEEP_BOTH",
  "why": "Directly gates whether facts get superseded/merged/removed at all; intentional safety behavior but hardcoded (F031_SAFETY_FLOOR_DESCRIPTION and an eval rubric depend on the literal), so it cannot be tuned when the resolver model's confidence calibration shifts.",
  "suggested": "contradiction_confidence_floor",
  "verify_evidence": "sleep_handler.py:1069-1071 downgraded_by_floor = confidence < 0.7 and raw_action != 'KEEP_BOTH' \u2014 literal safety floor gating SUPERSEDE/MERGE/REMOVE application to heart.facts; no Settings field (grep contradiction_ in config.py shows only detection/model/cooldown/similarity fields)."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 931,
  "constant": "score < 0.80",
  "stage": "persist",
  "impact": "medium",
  "what": "Cosine threshold for UPDATES:-prefixed reflection facts to supersede an existing fact",
  "why": "Decides between superseding (deactivating) an existing fact vs storing a near-duplicate; not in Settings and inconsistent with the other dedup thresholds (0.85, 0.92, 0.95) in the same pipeline.",
  "suggested": "updates_supersede_threshold",
  "verify_evidence": "sleep_handler.py:931 literal 0.80 in _handle_updates_prefix deciding supersede_fact (:949) vs learn-as-new (:936); no updates/supersede threshold field in config.py. Inconsistency with 0.85 (knowledge_extractor:132), 0.92 (fact_dedup_threshold), 0.95 (fact_native_cosine_threshold) confirmed."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 870,
  "constant": "[:20]",
  "stage": "derive",
  "impact": "low",
  "what": "Orient context caps at 20 existing facts (built from queries truncated to line[:100] at line 849, max 5 queries at line 850, limit=5 per search at line 859)",
  "why": "The reflection LLM's 'do NOT re-extract these' knowledge is bounded by these literals; too-small orient context causes duplicate re-extraction of already-known facts.",
  "suggested": "reflection_orient_max_facts",
  "verify_evidence": "sleep_handler.py:870 facts_list [:20], plus :849 line[:100], :850 max 5 queries, :859 search_facts(limit=5) \u2014 all literals in _build_orient_context; too-small orient context causes duplicate re-extraction into heart.facts. No Settings field."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 1427,
  "constant": ".limit(5)",
  "stage": "derive",
  "impact": "low",
  "what": "Max fact clusters considered for F027 consolidation per sleep cycle",
  "why": "Cluster-consolidation throughput is 5 subjects/night regardless of backlog; mergeable clusters beyond the top 5 by size sit as memory bloat until later cycles.",
  "suggested": "cluster_consolidation_max_clusters",
  "verify_evidence": "sleep_handler.py:1427 .limit(5) literal on the cluster-selection query; the adjacent bounds ARE settings (cluster_consolidation_min_facts/max_facts, config.py:277-278, read at sleep_handler.py:1412-1413) \u2014 the 5-clusters-per-cycle cap is the only literal in the trio."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 1489,
  "constant": "max_tokens=1000",
  "stage": "derive",
  "impact": "medium",
  "what": "Token budget for the F027 multi-fact cluster-merge tool call",
  "why": "Same F031 truncation class on a call whose merged_content replaces up to max_facts source facts; a truncated response is classified llm_malformed and the merge silently doesn't happen \u2014 and unlike the pairwise path this budget was never empirically measured (comment says 'not separately measured').",
  "suggested": "cluster_merge_max_tokens",
  "verify_evidence": "sleep_handler.py:1489 max_tokens=1000 literal on the _CLUSTER_MERGE_SCHEMA tool call; empty merged_content is classified llm_malformed (:1514-1515) and the merge silently skips. Comment :1484-1488 confirms 'cluster path not separately measured'. No Settings field."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/sleep_handler.py",
  "line": 1677,
  "constant": "max_bridges=20",
  "stage": "derive",
  "impact": "low",
  "what": "Max bridge edges created per sleep cycle during graph densification",
  "why": "Caps cross-cluster connectivity growth in brain.graph_edges; every other densifier bound (max_facts/decisions/episodes/procedures/chunks) is a Settings field, this one is a literal.",
  "suggested": "graph_backfill_max_bridges",
  "verify_evidence": "sleep_handler.py:1677 discover_clusters(max_bridges=20) literal; the callee default is also a literal 20 (graph_densifier.py:1478). config.py has graph_backfill_max_facts/decisions/episodes/procedures (:813-816) and graph_backfill_max_chunks (:1663) but no bridges field \u2014 claim's asymmetry confirmed."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/correction_extractor.py",
  "line": 158,
  "constant": "transcript[-2000:]",
  "stage": "derive",
  "impact": "medium",
  "what": "Correction-principle extraction sees only the last 2000 chars of the transcript (summary also cut to [:2000] at line 157)",
  "why": "The stored rule fact (and possible auto-censor) is derived from a tail slice that may not contain the actual correction exchange if follow-up chatter came after it; a wrong or vague principle gets persisted as category='rule', which loads EVERY turn.",
  "suggested": "correction_transcript_tail_chars",
  "verify_evidence": "correction_extractor.py:156-158 summary json [:2000] + transcript[-2000:] literals feeding _EXTRACTION_PROMPT; resulting principle persisted as category='rule' fact via heart.learn (:118-126) and optional censor (:132-138). Only correction_extraction_enabled exists in config.py (:1135) \u2014 a kill-switch, not a size knob."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/correction_extractor.py",
  "line": 169,
  "constant": "max_tokens=512",
  "stage": "derive",
  "impact": "low",
  "what": "Token budget for correction-principle extraction JSON",
  "why": "F031 class: truncated JSON fails parse and the correction lesson is silently never learned; 512 is tight for principle + subject + censor_pattern + reasoning drift.",
  "suggested": "correction_extraction_max_tokens",
  "verify_evidence": "correction_extractor.py:169 max_tokens=512 literal; parse failure returns None (:174-177) and the correction is silently never learned. No Settings field."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/correction_extractor.py",
  "line": 114,
  "constant": "len(principle) < 30",
  "stage": "persist",
  "impact": "low",
  "what": "Minimum length gate for storing a correction principle as a fact",
  "why": "Short but valid rules ('Never use rebase -i') are silently discarded; threshold not in Settings.",
  "suggested": "correction_min_principle_chars",
  "verify_evidence": "correction_extractor.py:113-115 'if not principle or len(principle) < 30: continue' \u2014 literal gate before the heart.learn write; no min-principle-chars field in config.py."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/outcome_detector.py",
  "line": 134,
  "constant": "transcript[-2000:]",
  "stage": "derive",
  "impact": "medium",
  "what": "Outcome-signal classification sees only last 2000 chars of transcript (summary dumped [:2000] at line 133; max_tokens=512 at line 143)",
  "why": "Persisted OutcomeSignal rows (corrected/completed/praised) drive rubric evolution and trigger CorrectionExtractor's fact/censor writes; a mid-conversation correction outside the 2000-char tail is never detected, so the correction-learning loop silently misses it.",
  "suggested": "outcome_detection_transcript_tail_chars",
  "verify_evidence": "outcome_detector.py:132-135 summary[:2000] + transcript[-2000:] literals (max_tokens=512 at :143); resulting OutcomeSignal rows are persisted (:95-104) and re-emitted to CorrectionExtractor (:112-122) which writes heart.facts/censors. Only rubric_outcome_detection_enabled (config.py:1139) and rubric_outcome_model exist \u2014 no tail-size field."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/procedure_learner.py",
  "line": 570,
  "constant": "score > 0.85",
  "stage": "persist",
  "impact": "medium",
  "what": "Cosine dedup threshold suppressing auto-procedure creation when a similar procedure exists",
  "why": "Gates every K-line procedure write (heart.procedures); this exact site previously suppressed ALL auto-procedure creation when it compared RRF scores \u2014 the threshold is load-bearing for whether procedural memory grows at all, yet is a literal while the clustering thresholds beside it are Settings fields.",
  "suggested": "procedure_dedup_threshold",
  "verify_evidence": "procedure_learner.py:569-572 literal 0.85 cosine gate in _is_duplicate suppressing heart.procedures writes; adjacent clustering bound procedure_cluster_min_size IS a settings field (used at :250) \u2014 the dedup threshold is not. No procedure_dedup field in config.py."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/procedure_learner.py",
  "line": 585,
  "constant": "max_tokens=1500",
  "stage": "derive",
  "impact": "medium",
  "what": "Token budget for extracting a full procedure JSON (name/domain/description/goals/patterns/tools/concepts/notes) from a decision or lesson cluster",
  "why": "F031 class: truncated JSON fails parse_llm_json \u2192 procedure silently not learned, or later fields (implementation_notes, core_concepts) are lost from the stored procedure.",
  "suggested": "procedure_extraction_max_tokens",
  "verify_evidence": "procedure_learner.py:585 max_tokens=1500 literal in _call_llm (shared by procedure-extraction prompts); invalid/truncated JSON returns None (:592-598) so the procedure is silently not learned. No Settings field."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/procedure_learner.py",
  "line": 234,
  "constant": "_TOTAL_WINDOW = 100",
  "stage": "derive",
  "impact": "low",
  "what": "Candidate pool of reviewed decisions for procedure clustering (episode-lesson pool similarly limit=50 at line 373)",
  "why": "Clusters that span older-than-window decisions never reach min_size, so their reusable pattern is never distilled into a procedure.",
  "suggested": "procedure_decision_window",
  "verify_evidence": "procedure_learner.py:234 _TOTAL_WINDOW = 100 local literal bounding the reviewed-decision pool (:235-244); episode-lesson pool list_episodes(limit=50) literal at :373. Neither is a Settings field (only procedure_cluster_min_size etc. exist)."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/decision_graph_linker.py",
  "line": 87,
  "constant": "timedelta(days=30)",
  "stage": "derive",
  "impact": "medium",
  "what": "Only facts learned / episodes started within 30 days are candidates for evidence_for / discussed_in edges to a new decision (both capped LIMIT 5 at lines 98/142)",
  "why": "A decision drawing on knowledge older than 30 days gets no live cross-type edge to it, contributing to graph orphan rate and weakening graph-augmented recall; the paired thresholds are Settings but the recency window and fan-out are literals.",
  "suggested": "decision_linker_recency_days",
  "verify_evidence": "decision_graph_linker.py:87 cutoff = now - timedelta(days=30) literal, applied to both fact (:95) and episode (:139) candidate SQL; LIMIT 5 literals at :98/:142. The similarity thresholds ARE settings (graph_threshold_fact_decision :86, graph_threshold_fact_episode :131) but window and fan-out are literals. Writes brain.graph_edges via create_edge (:118/:161)."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/episode_summarizer.py",
  "line": 238,
  "constant": "len(transcript) < 50",
  "stage": "derive",
  "impact": "low",
  "what": "Minimum transcript length for an episode to be summarized at all",
  "why": "Sub-50-char sessions never get a structured_summary, so no candidate facts, no chunks, no graph links \u2014 usually correct, but the gate is a literal duplicated at outcome_detector.py:76.",
  "suggested": "episode_min_transcript_chars",
  "verify_evidence": "episode_summarizer.py:238 literal 50 gate skipping summarization entirely (no summary, no candidate facts, no chunks); duplicated literal at outcome_detector.py:76. NOUS_EPISODE_CHUNK_MIN_TRANSCRIPT_CHARS (F067) is a different, chunking-only gate and is not consulted here."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/episode_summarizer.py",
  "line": 707,
  "constant": "[:10]",
  "stage": "persist",
  "impact": "low",
  "what": "Merged multi-chunk episode summaries persist at most 10 key_points (open_threads similarly [:10] at line 717)",
  "why": "Long multi-chunk episodes lose lessons beyond the first 10 chunks' worth; key_points feed procedure learning (lessons_learned clustering) so dropped points never become procedures.",
  "suggested": "episode_max_key_points",
  "verify_evidence": "episode_summarizer.py:707 merged_key_points[:10] and :717 merged_open_threads[:10] literals in _merge_summaries; result persisted to heart.episodes.structured_summary via update_episode_summary (:247). Note candidate_facts in the same merge ARE settings-capped via cap_candidate_facts (:700-702) \u2014 key_points/open_threads are not."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/episode_summarizer.py",
  "line": 477,
  "constant": "LIMIT 3",
  "stage": "derive",
  "impact": "low",
  "what": "Max similar episodes linked (related_to edges) per newly summarized episode",
  "why": "Caps episode\u2194episode graph density at write time; an episode similar to many others gets edges to only the top 3, and the cap is a SQL literal unlike the threshold beside it which is a Settings field.",
  "suggested": "episode_link_max_neighbors",
  "verify_evidence": "episode_summarizer.py:477 SQL 'LIMIT 3' literal in _link_similar_episodes; the threshold beside it is settings (graph_threshold_episode_episode, :465). Writes episode\u2194episode related_to edges to brain.graph_edges (:489-494)."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/decision_reviewer.py",
  "line": 64,
  "constant": "confidence < 0.4",
  "stage": "derive",
  "impact": "medium",
  "what": "Auto-labels any decision recorded below 0.4 confidence as outcome='failure' (accepted because signal confidence 0.9 >= CONFIDENCE_THRESHOLD 0.7 at line 192)",
  "why": "Writes permanent review outcomes onto brain.decisions from a hardcoded prior, feeding Brain calibration (Brier scores) with labels derived from the very confidence value being calibrated \u2014 threshold shifts (e.g. after F058's 0.7627 write-time scaling of confidence) silently change how many decisions get auto-failed.",
  "suggested": "auto_review_low_confidence_threshold",
  "verify_evidence": "decision_reviewer.py:64 literal 0.4 in ErrorSignal.check returns failure with signal confidence 0.9, which clears CONFIDENCE_THRESHOLD=0.7 (module literal at :192); outcome persisted onto brain.decisions via self._brain.review (:273-278 and sweep :294-299, reviewer='auto'). No auto_review/low-confidence field in config.py. ErrorSignal is unconditionally wired (:217-219)."
 },
 {
  "area": "handlers",
  "file": "nous/handlers/actionability_backfill.py",
  "line": 41,
  "constant": "_TOKENS_PER_LLM_CALL = 250",
  "stage": "derive",
  "impact": "low",
  "what": "Estimate converting the F047 token budget into a hard cap on LLM classification calls during backfill",
  "why": "If the estimate is low relative to real usage the budget overspends; if high, facts fall through to the default (fail-closed actionable=false) verdict persisted on heart.facts, silently muting heartbeat surfacing for those rows.",
  "suggested": "actionability_tokens_per_call_estimate",
  "verify_evidence": "actionability_backfill.py:39-41 class literal 250 divides the (settings-wired) budget into a hard call cap at :55; budget exhaustion routes tier-2 to the default verdict, still persisted on heart.facts.actionable via _update_actionable (:137), with the exhaustion warning at :165-174 confirming the fall-through. token_budget itself IS configurable (main.py:240 threads settings.actionability_backfil"
 },
 {
  "area": "brain",
  "file": "nous/brain/brain.py",
  "line": 306,
  "constant": "len(desc_lower) < 20",
  "stage": "capture",
  "impact": "medium",
  "what": "Hard-rejects decisions with descriptions under 20 chars and no reasons \u2014 the decision is never persisted to brain.decisions (ValueError raised in _record).",
  "why": "A terse-but-real decision from the agent is permanently lost at capture; downstream calibration, graph linking, and recall never see it, and the threshold is invisible to operators (not in Settings).",
  "suggested": "noise_decision_min_description_chars",
  "verify_evidence": "brain.py:306 hard filter inside _is_noise_decision; _record raises ValueError at brain.py:341-342 so the decision never reaches brain.decisions. No Settings wiring: grep of nous/config.py for noise/min_description/filler returns nothing; _is_noise_decision takes no settings and Brain.__init__ (brain.py:69-74) does not thread any threshold in."
 },
 {
  "area": "brain",
  "file": "nous/brain/brain.py",
  "line": 319,
  "constant": "len(desc_stripped) < 60 + _FILLER_PREFIXES",
  "stage": "capture",
  "impact": "medium",
  "what": "Hard-rejects any sub-60-char decision starting with 'let me', \"i'll\", 'i will', 'great', etc. \u2014 never stored in brain.decisions.",
  "why": "Legitimate short first-person decisions (\"I'll migrate the DB to pgvector\") are silently dropped at capture; the decision record, its graph edges, and its calibration signal are lost forever.",
  "suggested": "noise_decision_filler_max_chars",
  "verify_evidence": "brain.py:317-320: _FILLER_PREFIXES tuple is a function-local literal and the 60-char bound is inline; same hard-reject path via ValueError at brain.py:341-342. No related field in nous/config.py (grep for filler/noise: no matches)."
 },
 {
  "area": "brain",
  "file": "nous/brain/brain.py",
  "line": 329,
  "constant": "> 0.5",
  "stage": "capture",
  "impact": "medium",
  "what": "Hard-rejects decisions whose word set is >50% _NOISE_KEYWORDS (also a hardcoded frozenset at brain.py:60) when no reasons given \u2014 never persisted.",
  "why": "Short status-flavored but genuine decisions are dropped before storage; the noise-keyword list and ratio are untunable, so per-deployment vocabulary drift silently changes what enters memory.",
  "suggested": "noise_decision_keyword_ratio",
  "verify_evidence": "brain.py:327-329 ratio check against _NOISE_KEYWORDS frozenset defined at brain.py:60-63 (module-level literal). Hard reject via brain.py:341-342. Neither the 0.5 ratio nor the keyword set has any Settings/env counterpart in nous/config.py."
 },
 {
  "area": "brain",
  "file": "nous/brain/graph_linker.py",
  "line": 194,
  "constant": "timedelta(days=30)",
  "stage": "derive",
  "impact": "high",
  "what": "Live fact\u2192decision evidence_for linking only considers decisions created in the last 30 days; older decisions can NEVER receive an evidence_for edge from a newly learned fact via this path.",
  "why": "Graph connectivity to decisions older than 30 days is structurally impossible at learn time, so Path-A expansion, adjacency boost, and spreading activation permanently miss fact-evidence for long-lived decisions (only the capped sleep backfill can partially compensate).",
  "suggested": "cross_type_link_decision_window_days",
  "verify_evidence": "graph_linker.py:194 cutoff = now - timedelta(days=30), bound as :cutoff in the candidate SQL at graph_linker.py:203 (created_at >= :cutoff on brain.decisions). Production path: handlers/fact_graph_linker.py:58 calls link_fact_to_decisions on every learned fact. Edges persist to brain.graph_edges (pg_insert at graph_linker.py:228-241). No window-days field exists in nous/config.py (only cross_type_"
 },
 {
  "area": "brain",
  "file": "nous/brain/graph_linker.py",
  "line": 207,
  "constant": "LIMIT 5",
  "stage": "derive",
  "impact": "medium",
  "what": "Max 5 candidate facts considered when linking a new fact to existing facts (related_to edges).",
  "why": "Caps fact\u2194fact graph density formed at learn time; facts in dense topical clusters lose associative edges that Path-A expansion and adjacency boost would otherwise traverse.",
  "suggested": "cross_type_link_max_candidates",
  "verify_evidence": "graph_linker.py:207 literal LIMIT 5 in the fact\u2192decision candidate SQL; edges persisted at graph_linker.py:228-241. Live path handlers/fact_graph_linker.py:58. No candidate-count field in nous/config.py (grep candidate_limit/cross_type: only thresholds + min_content_chars exist)."
 },
 {
  "area": "brain",
  "file": "nous/brain/graph_linker.py",
  "line": 302,
  "constant": "LIMIT 5",
  "stage": "derive",
  "impact": "medium",
  "what": "Max 5 candidate facts considered when linking a new fact to existing facts (related_to edges).",
  "why": "Caps fact\u2194fact graph density formed at learn time; facts in dense topical clusters lose associative edges that Path-A expansion and adjacency boost would otherwise traverse.",
  "suggested": "cross_type_link_max_candidates",
  "verify_evidence": "graph_linker.py:302 literal LIMIT 5 in link_fact_to_facts candidate SQL; related_to edges persisted at graph_linker.py:316-330. Live path handlers/fact_graph_linker.py:63. Same absence of any Settings field."
 },
 {
  "area": "brain",
  "file": "nous/brain/graph_linker.py",
  "line": 213,
  "constant": "* 0.9",
  "stage": "derive",
  "impact": "low",
  "what": "Hardcoded relaxation factor for the SQL pre-filter before per-candidate re-embed verification (same pattern at line 308 with cross_type_same_threshold, and graph_densifier.py:343).",
  "why": "Candidates whose stored-embedding similarity is below 0.9x threshold but whose common-template similarity would pass are pre-filtered out, silently narrowing which edges can form regardless of the configured threshold.",
  "suggested": "cross_type_prefilter_relaxation",
  "verify_evidence": "Hardcoded 0.9 relaxation at graph_linker.py:213 (cross_type_threshold * 0.9), graph_linker.py:308 (cross_type_same_threshold * 0.9), and graph_densifier.py:343 (pre_threshold = threshold * 0.9). Base thresholds ARE configurable (config.py:719-720, NOUS_CROSS_TYPE_THRESHOLD / graph_threshold_*), but the pre-filter factor itself has no Settings/env counterpart. Claim direction verified: stored-embed"
 },
 {
  "area": "brain",
  "file": "nous/brain/graph_linker.py",
  "line": 121,
  "constant": "RELATION_WEIGHT_MULTIPLIERS.get(relation, 0.8)",
  "stage": "persist",
  "impact": "medium",
  "what": "Per-relation weight discounts (dict at lines 29-45: discussed_in/extracted_from 0.7, related_to 0.8, etc., default 0.8) permanently baked into the persisted brain.graph_edges.weight.",
  "why": "Stored edge weights are consumed by _neighbors ranking, spreading activation, and adjacency boost; the discount table is untunable and mixes score spaces (raw cosine vs discounted cosine vs edge_confidence), feeding the known R2 score-space incoherence.",
  "suggested": "graph_relation_weight_multipliers (JSON dict)",
  "verify_evidence": "Module-level dict at graph_linker.py:29-45, applied at graph_linker.py:118-123, persisted into brain.graph_edges.weight at graph_linker.py:134. Nuance the scanner missed (does not overturn verdict): only edges routed through create_edge get the discount \u2014 densifier backfill (graph_densifier.py:283, 429) and F070 chunk paths \u2014 while the live fact\u2192decision/fact\u2192fact linkers bypass create_edge and st"
 },
 {
  "area": "brain",
  "file": "nous/brain/graph_linker.py",
  "line": 59,
  "constant": "similarity * 0.6 (+0.05/+0.15/+0.10/+0.001 signal weights)",
  "stage": "persist",
  "impact": "medium",
  "what": "edge_confidence formula weights. Sole live caller is graph_densifier.py:422 with shared_tags=0, shared_subject=False, temporal_proximity_days=0.0 \u2014 so every cross-type backfill edge persists weight = similarity*0.6 + 0.15.",
  "why": "Cross-type backfill edges are stored in a compressed weight scale (0.75 cosine -> 0.60 stored) while same-type edges store raw cosine, permanently distorting relative ranking for every downstream graph consumer (neighbors ordering, adjacency boost, spreading).",
  "suggested": "edge_confidence_similarity_weight",
  "verify_evidence": "Formula literals at graph_linker.py:59-62. Sole caller verified by grep: graph_densifier.py:422-427 with shared_tags=0, shared_subject=False, temporal_proximity_days=0.0 \u2014 so every cross-type backfill edge weight = similarity*0.6 + 0.15 exactly as claimed, then persisted via create_edge (graph_densifier.py:429-436) where it is FURTHER multiplied by RELATION_WEIGHT_MULTIPLIERS (graph_linker.py:123)"
 },
 {
  "area": "brain",
  "file": "nous/brain/graph_densifier.py",
  "line": 219,
  "constant": "[:500]",
  "stage": "derive",
  "impact": "medium",
  "what": "Orphan node content truncated to 500 chars before the hybrid (FTS) search that selects same-type edge candidates during sleep backfill (same slice at line 355 for cross-type keyword search).",
  "why": "For long episode summaries / documents-as-facts, keyword candidate discovery only sees the first 500 chars, so edges to nodes sharing terms in the tail are never proposed \u2014 a permanent gap in graph edges (the backfill marks the node non-orphan afterwards and never revisits).",
  "suggested": "graph_backfill_query_text_chars",
  "verify_evidence": "graph_densifier.py:219 query_text=orphan_content[:500] for same-type hybrid candidate search, and graph_densifier.py:355 orphan_content[:500] for the cross-type keyword leg. Precisely as claimed: only the keyword/FTS discovery leg is truncated (the vector leg uses the full stored embedding at :218). Surviving candidates become persisted brain.graph_edges via create_edge (:283, :429). No graph_back"
 },
 {
  "area": "brain",
  "file": "nous/brain/graph_densifier.py",
  "line": 222,
  "constant": "limit=10",
  "stage": "derive",
  "impact": "medium",
  "what": "Same-type backfill candidate pool capped at 10 hybrid-search hits per orphan (with hardcoded 0.6 vector weight at line 224); cross-type legs are capped at 5 (SQL LIMIT 5 at line 338, keyword limit=5 at line 357).",
  "why": "Bounds how many edges an orphan can ever gain in its single backfill pass; since edge existence (not weight) drives graph-leg retrieval, candidates ranked 11+ are permanently unlinked.",
  "suggested": "graph_backfill_candidate_limit",
  "verify_evidence": "limit=10 at graph_densifier.py:223 (same-type hybrid pool), SQL LIMIT 5 at graph_densifier.py:338 (cross-type vector leg), limit=5 at graph_densifier.py:357 (cross-type keyword leg). config.py graph_backfill_max_facts/decisions/episodes/procedures/chunks (812-816, 1663) cap orphan batch size per cycle, NOT per-orphan candidate pool \u2014 no candidate-limit field exists. Edges persist to brain.graph_ed"
 },
 {
  "area": "brain",
  "file": "nous/brain/graph_densifier.py",
  "line": 63,
  "constant": "0.75",
  "stage": "derive",
  "impact": "low",
  "what": "Fallback cosine threshold for type pairs missing from the strict-threshold table (e.g. decision\u2194episode discussed_in backfill); CE-mode twin at line 81 falls back to 0.60.",
  "why": "Unmapped type-pair edges are gated by an invisible threshold that ignores the operator's NOUS_GRAPH_THRESHOLD_* tuning, so decision-episode connectivity is fixed regardless of config.",
  "suggested": "graph_threshold_default",
  "verify_evidence": "graph_densifier.py:63 thresholds.get(key, 0.75) in _get_strict_threshold; CE-mode twin fallback 0.60 at graph_densifier.py:81. The threshold tables (:54-60, :72-78) omit the (decision, episode) pair, so decision\u2194episode discussed_in backfill is gated by the hardcoded fallback regardless of every NOUS_GRAPH_THRESHOLD_* / NOUS_CE_BACKFILL_THRESHOLD_* setting. No graph_threshold_default field in nous"
 },
 {
  "area": "brain",
  "file": "nous/brain/graph_densifier.py",
  "line": 1491,
  "constant": ".days < 7 (+ max_bridges=20 at :1478, len(nodes) >= 3 at :1540)",
  "stage": "derive",
  "impact": "low",
  "what": "Cluster discovery (bridge edges between disconnected graph components) runs at most weekly, creates at most 20 bridges, and only considers components with 3+ nodes.",
  "why": "Cross-cluster connectivity in brain.graph_edges accrues at a hardcoded ceiling (20/week), and 2-node components can never be bridged, leaving small memory islands unreachable by graph traversal.",
  "suggested": "graph_cluster_discovery_interval_days / graph_cluster_max_bridges",
  "verify_evidence": "graph_densifier.py:1489-1491 rate limit .days < 7; max_bridges default 20 at :1478 AND the sole production caller passes the literal \u2014 sleep_handler.py:1677 discover_clusters(max_bridges=20) \u2014 so the default param is not a config seam; 3+ node component filter at graph_densifier.py:1539-1541. Bridge edges persist to brain.graph_edges. No cluster-discovery interval/bridge-count field in nous/config"
 },
 {
  "area": "brain",
  "file": "nous/brain/bridge.py",
  "line": 14,
  "constant": "_MAX_STRUCTURE_LEN = 200",
  "stage": "derive",
  "impact": "low",
  "what": "Bridge structure/function derived from a decision is only the FIRST sentence, truncated to 200 chars, before being persisted to brain.decision_bridge (truncation applied at line 26).",
  "why": "The stored bridge text is the search surface for Brain.query's bridge_side ILIKE filter; content past the first sentence / 200 chars is unsearchable via bridge-side retrieval forever.",
  "suggested": "bridge_max_structure_chars",
  "verify_evidence": "bridge.py:14 module constant, truncation applied at bridge.py:25-26 inside _first_sentence; BridgeExtractor is instantiated with no settings (brain.py:81) and its output is persisted to brain.decision_bridge at brain.py:363+392 (record path) and brain.py:526-531 (re-extract path). The stored text is the search surface for bridge_side ILIKE at brain.py:720-723, so post-truncation content is unsearc"
 },
 {
  "area": "brain",
  "file": "nous/brain/graph_densifier.py",
  "line": 224,
  "constant": "vector_weight=0.6",
  "stage": "derive",
  "impact": "low",
  "what": "Fixed vector-vs-keyword fusion weight for orphan-backfill candidate selection, bypassing the runtime-tunable search weights used elsewhere (/admin/search-weights).",
  "why": "Which candidates surface for permanent edge creation is decided by a fusion weight the operator cannot tune, diverging from the recall-path weighting and skewing which memories get connected.",
  "suggested": "graph_backfill_vector_weight",
  "verify_evidence": "graph_densifier.py:224 passes vector_weight=0.6 explicitly to hybrid_search, and hybrid_search's documented weight-resolution order (heart/search.py:190, 203-206) gives the explicit param HIGHEST priority \u2014 above both the /admin/search-weights runtime override and NOUS_VECTOR_WEIGHT / config.py:114 vector_weight=0.7 \u2014 so operator tuning is bypassed exactly as claimed. Candidates selected here beco"
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\retrieval_pipeline.py",
  "line": 456,
  "constant": "[:3]",
  "stage": "retrieve",
  "impact": "medium",
  "what": "Stage 2b chunk-seed cap: only the top-3 F067 chunk hits fan out to graph neighbors, even though the chunk leg retrieves up to episode_chunk_recall_limit (default 10).",
  "why": "Chunks have the richest F070 same-episode neighborhoods; 70% of retrieved chunks contribute nothing to graph expansion, weakening the verbatim-recall path the chunks feature exists for.",
  "suggested": "NOUS_HEART_GRAPH_CHUNK_SEED_COUNT",
  "verify_evidence": "retrieval_pipeline.py:456 `for hr in acc.heart_results[:3]:` \u2014 literal slice, no settings read. Grep of nous/config.py for seed-count fields found only heart_graph_neighbors_per_seed (config.py:1541, per-seed breadth) and proc_graph_neighbors_per_seed (config.py:188, procedures) \u2014 no seed-COUNT field anywhere. Function is run_recall_pipeline's internal stage; no caller can override the slice."
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\retrieval_pipeline.py",
  "line": 469,
  "constant": "limit=2",
  "stage": "retrieve",
  "impact": "medium",
  "what": "Max 2 decision neighbors fetched per Heart seed in Stage 2 cross-type expansion (contrast: the brain-side leg uses settings.graph_recall_max_neighbors, default 3).",
  "why": "A fact linked to 3+ relevant decisions surfaces only 2 of them, and the cap is invisible to operators tuning NOUS_GRAPH_RECALL_MAX_NEIGHBORS, which does not apply here.",
  "suggested": "reuse graph_recall_max_neighbors or NOUS_HEART_GRAPH_DECISION_NEIGHBORS",
  "verify_evidence": "retrieval_pipeline.py:466-471 `brain.neighbors(hr.id, node_type=hr.type, limit=2, neighbor_type=\"decision\")` \u2014 literal 2. Contrast confirmed: settings.graph_recall_max_neighbors (config.py:646) is consumed ONLY at retrieval_pipeline.py:680 (brain-side 1-hop leg); grep shows no other consumer. NOUS_GRAPH_RECALL_MAX_NEIGHBORS does not reach this call, exactly as claimed."
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\retrieval_pipeline.py",
  "line": 510,
  "constant": "[:3]",
  "stage": "retrieve",
  "impact": "medium",
  "what": "Stage 2b chunk-seed cap: only the top-3 F067 chunk hits fan out to graph neighbors, even though the chunk leg retrieves up to episode_chunk_recall_limit (default 10).",
  "why": "Chunks have the richest F070 same-episode neighborhoods; 70% of retrieved chunks contribute nothing to graph expansion, weakening the verbatim-recall path the chunks feature exists for.",
  "suggested": "NOUS_HEART_GRAPH_CHUNK_SEED_COUNT",
  "verify_evidence": "retrieval_pipeline.py:509-512 mem_seeds built from `acc.heart_results[:3]` \u2014 literal slice. Per-seed breadth IS settings-wired one line up (`mem_limit = max(1, int(settings.heart_graph_neighbors_per_seed))` at :495, config.py:1541), confirming the scanner's asymmetry claim: breadth configurable, seed count not. No Settings field for seed count exists in config.py."
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\retrieval_pipeline.py",
  "line": 519,
  "constant": "[:3]",
  "stage": "retrieve",
  "impact": "medium",
  "what": "Stage 2b chunk-seed cap: only the top-3 F067 chunk hits fan out to graph neighbors, even though the chunk leg retrieves up to episode_chunk_recall_limit (default 10).",
  "why": "Chunks have the richest F070 same-episode neighborhoods; 70% of retrieved chunks contribute nothing to graph expansion, weakening the verbatim-recall path the chunks feature exists for.",
  "suggested": "NOUS_HEART_GRAPH_CHUNK_SEED_COUNT",
  "verify_evidence": "retrieval_pipeline.py:516-520 `for item in acc.chunk_results[:3]` \u2014 literal slice on the chunk-seed extend. Chunk leg retrieves up to settings.episode_chunk_recall_limit (config.py:1302, used at retrieval_pipeline.py:423-425), so retrieved-vs-seeded mismatch claim is accurate. No config field caps or widens the chunk seed count."
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\retrieval_pipeline.py",
  "line": 648,
  "constant": "activation > 0.1",
  "stage": "retrieve",
  "impact": "low",
  "what": "Spreading-activation results below 0.1 activation are dropped from graph_expanded before the LLM ever sees them.",
  "why": "A hidden floor on the multi-hop leg (currently auto-off in prod at density 2.745<3.0) that cannot be tuned when spreading activation is enabled, silently discarding weakly-activated but possibly relevant nodes.",
  "suggested": "NOUS_SPREADING_ACTIVATION_RESULT_FLOOR",
  "verify_evidence": "retrieval_pipeline.py:644-649: `if (nid not in seed_ids and nid not in seen_ids and activation > 0.1)` \u2014 literal 0.1 floor. config.py:740-747 defines spreading_activation_* (enabled/density_threshold/decay/max_depth/alpha/beta/gamma) but NO result floor field. Only bites when spreading activation runs (density-gated, currently auto-off in prod at 2.745<3.0), matching the scanner's low-impact frami"
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\retrieval_pipeline.py",
  "line": 654,
  "constant": "str(nid)[:8]",
  "stage": "retrieve",
  "impact": "medium",
  "what": "Spreading-activation results carry only an 8-char UUID prefix as their description \u2014 no content is ever fetched for them.",
  "why": "Every spreading-activation hit is delivered to the LLM as a contentless placeholder like '[fact] a1b2c3d4', so the entire multi-hop leg conveys zero information downstream (matches the known content-placeholder fusion bug from PR #543 review).",
  "suggested": "",
  "verify_evidence": "retrieval_pipeline.py:650-658: NeighborResult constructed with `description=f\"[{ntype}] {str(nid)[:8]}\"`; no later hydration exists \u2014 _graph_expanded_to_pipeline (:1146-1170) passes `n.description` straight into PipelineResult (:1154) with source=\"graph_expanded\"/\"spreading_activation\" (:1159), so the LLM receives the contentless placeholder verbatim. Note this is a missing-content-fetch bug, not "
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\retrieval_pipeline.py",
  "line": 1201,
  "constant": "stale_penalty: float = 0.3",
  "stage": "retrieve",
  "impact": "medium",
  "what": "Down-rank multiplier applied to the superseded fact in \u00a71 recency-conflict resolution; the sole call site (line 278: _resolve_recency_conflicts(results, settings)) never overrides it, so it is effectively hardcoded.",
  "why": "Recency resolver is ON in prod; a fixed 0.3 collapse of a superseded fact's score is untunable and, combined with rerank_by_score paths, controls whether stale facts drop out of top-K for the LLM.",
  "suggested": "NOUS_RECENCY_RESOLVER_STALE_PENALTY",
  "verify_evidence": "retrieval_pipeline.py:1201 keyword-only default; applied at :1289 `score=(r.score or 0.0) * stale_penalty`. Sole call site :278 `_resolve_recency_conflicts(results, settings)` never overrides (grep confirms no other caller; cognitive/context.py:1317 only mentions it in a comment). config.py wires recency_resolver_similarity_floor (read at :1218) but has NO stale-penalty field. Nuance per the docst"
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\tools.py",
  "line": 1041,
  "constant": "[:160]",
  "stage": "persist",
  "impact": "low",
  "what": "Summary text of each surfaced recall result persisted via heart._residual_activator.record_surfaced (F055 working-memory rows used for cross-turn residual activation).",
  "why": "The persisted surfaced-item summary is capped at 160 chars, so any downstream consumer of the residual-activation record (heart.working_memory) sees a truncated stand-in for the memory item.",
  "suggested": "NOUS_RESIDUAL_SURFACED_SUMMARY_CHARS",
  "verify_evidence": "tools.py:1036-1044 builds surfaced tuples with `(r.description or \"\")[:160]` \u2192 heart._residual_activator.record_surfaced (:1045-1052) \u2192 residual_activation.py:260 persists `snippet[:160]` into heart.working_memory JSONB items (real DB persist, F055). Double-truncation: even overriding the tools.py cap would be re-clamped at residual_activation.py:260. config.py:1156-1190 residual_* fields (top_k_c"
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\tools.py",
  "line": 1176,
  "constant": "body[:150]",
  "stage": "retrieve",
  "impact": "medium",
  "what": "recall_recent truncates each episode's summary body to 150 chars in the text returned to the LLM.",
  "why": "recall_recent is a primary follow-up/recap surface (known weak per the 2026-06-19 follow-up-association analysis); 150 chars of a multi-hundred-char structured summary strips open threads and specifics the agent then re-learns or answers from.",
  "suggested": "NOUS_RECALL_RECENT_BODY_CHARS",
  "verify_evidence": "tools.py:1175-1176 `if body and body != title: lines.append(f\"  {body[:150]}\")` inside recall_recent \u2014 literal 150, no settings object in scope for this value. No related field in config.py (grep for recall_recent/recent_body: no hits). Caveat: this caps the retrieval text the LLM reads (memory read path), never stored data \u2014 memory-relevant under the scanner's retrieve-stage scope."
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\tools.py",
  "line": 1171,
  "constant": "body[:60]",
  "stage": "retrieve",
  "impact": "low",
  "what": "Fallback title for un-titled episodes in recall_recent output shown to the LLM is the first 60 chars of the summary body.",
  "why": "For unsummarized episodes (body = raw creation-time echo) the agent identifies the episode by a 60-char fragment, which can misrepresent the episode topic in recap answers.",
  "suggested": "",
  "verify_evidence": "tools.py:1171 `title = structured.get(\"title\") or e.title or (body[:60] if body else \"Untitled\")` \u2014 literal 60, fallback only (fires when both structured title and legacy title are absent). No config field. Mitigating detail the scanner underweights: the ' (unsummarized)' marker (:1172) flags these rows, and body[:150] is still appended at :1176 when body != title, so the 60-char fragment is rarel"
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\tools.py",
  "line": 3173,
  "constant": "_MAX_WRITES = 5",
  "stage": "capture",
  "impact": "low",
  "what": "Hard cap of 5 learn_fact calls per run_python execution; the 6th raises RuntimeError.",
  "why": "A batch script legitimately extracting >5 facts (e.g. processing a document) fails mid-write and the remaining facts are never stored; the cap is advertised in the tool schema but not operator-tunable.",
  "suggested": "NOUS_PROGRAMMATIC_TOOLS_MAX_FACT_WRITES",
  "verify_evidence": "tools.py:3173 module-level constant; enforced at :3252-3253 (`raise RuntimeError(f\"learn_fact write cap ({_MAX_WRITES}) exceeded\")`) inside create_programmatic_tools' _learn_fact closure. The closure receives `settings` (:3179) but only reads programmatic_tools_timeout (:3222); config.py has only programmatic_tools_enabled/timeout (:632-633) \u2014 no write-cap field. Claim exact: 6th write raises mid-"
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\tools.py",
  "line": 3234,
  "constant": "limit: int = 5",
  "stage": "retrieve",
  "impact": "medium",
  "what": "run_python's in-scope recall_deep defaults to 5 results AND is silently facts-only (heart.search_facts) \u2014 no episodes, decisions, chunks, or graph legs, unlike the real recall_deep tool it is named after.",
  "why": "The tool schema tells the agent to batch memory lookups through run_python 'recall_deep', but that path retrieves a 5-fact slice of one memory type, so batched recalls systematically miss episodes/decisions the agent believes it searched.",
  "suggested": "NOUS_PROGRAMMATIC_RECALL_LIMIT (and route through run_recall_pipeline)",
  "verify_evidence": "tools.py:3234-3236: `def _recall_deep(query, limit=5): results = _schedule(heart.search_facts(query, limit=limit))` \u2014 confirmed facts-only (heart.search_facts), no episodes/decisions/chunks/graph, unlike the real recall_deep which runs run_recall_pipeline. No Settings field for the default (config.py programmatic_* = enabled/timeout only). Nuance: the AGENT can pass limit per call inside its scrip"
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\mcp.py",
  "line": 327,
  "constant": "content[:100]",
  "stage": "persist",
  "impact": "low",
  "what": "MCP nous_teach(type=procedure) derives the persisted procedure NAME from the first 100 chars of content at write time.",
  "why": "The truncated name is the dedup key (get_procedure_by_name) and the catalog label the agent uses to select skills, so two procedures sharing a 100-char prefix collide and the stored identity is lossy forever.",
  "suggested": "",
  "verify_evidence": "mcp.py:326-329: `name = content[:100].strip()` then word-boundary trim + '...' when len>100; persisted via heart.store_procedure (:335) into heart.procedures.name (models.py:605, String(500), no unique constraint). Literal 100, no settings. Minor overreach in the claim: THIS write path calls store_procedure directly with no get_procedure_by_name dedup check (heart.py:462-481 just stores + emits ev"
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\mcp.py",
  "line": 239,
  "constant": "args.get(\"limit\", 5) / min(..., 100)",
  "stage": "retrieve",
  "impact": "low",
  "what": "MCP nous_recall default retrieval depth (5) and hard ceiling (100) per memory leg for external MCP clients.",
  "why": "External agents querying Nous memory over MCP get a 5-item view by default \u2014 half the internal recall_deep default of 10 \u2014 shaping what other agents learn from Nous.",
  "suggested": "NOUS_MCP_RECALL_DEFAULT_LIMIT",
  "verify_evidence": "mcp.py:239 `limit = min(int(args.get(\"limit\", 5)), 100)` \u2014 literal default 5 and ceiling 100 (AS-10 hostile-client cap per comment :237-238), threaded to brain.query (:245) and heart.recall (:265/:280). MCP clients can pass their own limit up to 100, but neither the default nor the ceiling reads Settings (grep config.py for mcp_recall: no hits; only mcp_enabled exists)."
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\runner.py",
  "line": 1485,
  "constant": "confidence=0.3",
  "stage": "persist",
  "impact": "low",
  "what": "Fixed confidence (and fixed category 'technical') on every fact salvaged from hard-cleared tool results before pruning (streaming path; duplicate at runner.py:1990-1995 for the non-streaming loop).",
  "why": "confidence feeds the F023 admission composite and later retrieval ranking, so all pre-prune salvage facts enter memory with an untunable low-confidence prior regardless of content quality.",
  "suggested": "NOUS_PRE_PRUNE_FACT_CONFIDENCE",
  "verify_evidence": "runner.py:1482-1487 (streaming) and duplicate :1990-1995 (non-streaming): `heart.learn(FactInput(content=fact_text, category=\"technical\", confidence=0.3, source=\"pre_prune_extraction\"))` \u2014 literal 0.3 + fixed category at both sites, persisted to heart.facts. grep config.py for pre_prune/salvage: zero hits. Source tag 'pre_prune_extraction' is staleness-exempt (cognitive/context.py:68), so these lo"
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\compaction.py",
  "line": 751,
  "constant": "[:5]",
  "stage": "capture",
  "impact": "medium",
  "what": "Max 5 key-value patterns extracted from a tool result before it is metadata-degraded/hard-cleared; these strings are the input to heart.learn at runner.py:1482/1990.",
  "why": "This regex sweep is the ONLY salvage path for tool-result content before hard-clear destroys it \u2014 key-values beyond the 5th are lost to memory permanently (same early-seam poisoning class as the layer.py [:500] archetype).",
  "suggested": "NOUS_PRE_PRUNE_MAX_KV_FACTS",
  "verify_evidence": "compaction.py:751 `facts.extend(re.findall(r'\\b\\w+:\\s+[\\w.-]+', content)[:5])` in _extract_facts_before_clear (:743), invoked at :852 during hard-clear; returned strings flow to heart.learn via runner.py:1478-1487 and :1986-1995 \u2014 real memory-write path before the source text is destroyed. No settings read anywhere in the function; config.py has no kv/pre-prune fact-cap field (tool_pruning_* field"
 },
 {
  "area": "api",
  "file": "E:\\Projects\\nous\\nous\\api\\compaction.py",
  "line": 752,
  "constant": "[:10]",
  "stage": "capture",
  "impact": "medium",
  "what": "Total cap of 10 salvaged strings (URLs + file paths + key-values combined) per tool result before hard-clear.",
  "why": "A URL-heavy tool result (e.g. web_search with 10+ links) fills the cap with URLs alone, crowding out file paths and key-values, and everything past item 10 never reaches heart.facts before the source text is destroyed.",
  "suggested": "NOUS_PRE_PRUNE_MAX_FACTS",
  "verify_evidence": "compaction.py:752 `return facts[:10]` \u2014 total cap across URLs (:747) + file paths (:749) + key-values (:751), in that append order, so the URL-crowding-out claim is structurally correct (URLs fill the list first). Same heart.learn persistence path as the [:5] finding (runner.py:1478-1487/:1986-1995). No Settings wiring; grep of config.py for pre_prune/salvage caps: zero hits."
 }
]