"""F092: A2UI Companion — agent-composed interactive surfaces.

Declarative A2UI v1.0 envelopes (vendored schemas under ``catalogs/``)
pushed over SSE to the Svelte companion renderer at /companion, with user
actions returning through an allowlisted, nonce-checked action router.
"""
